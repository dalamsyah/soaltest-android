<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>
        <variable name="detail" type="com.dimasalamsyah.soaltest.TransaksiDetailModel"/>
    </data>

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:orientation="vertical"
        android:id="@+id/layout"
        android:padding="10dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:text="@{`Kode Barang: `+detail.barang.kodebarang}"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

        <TextView
            android:text="@{`Nama Barang: `+detail.barang.namabarang}"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

        <TextView
            android:text="@{`Jumlah Barang: `+detail.jumlahbarang}"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>


    </LinearLayout>
</layout>